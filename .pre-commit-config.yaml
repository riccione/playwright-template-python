repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version matches your dependency preference
    rev: v0.15.20
    hooks:
      # Run the linter.
      - id: ruff-check
        args: [ --fix ]
      # Run the formatter.
      - id: ruff-format
